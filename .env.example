# Fulloch Environment Variables
# Copy this file to .env and update with your values.
# These values are loaded automatically via python-dotenv.

# =============================================================================
# API Server
# =============================================================================
# Optional override for the API bind port in docker-compose.
# FULLOCH_PORT=8000
# Optional override for API auth token (same as api.api_key in config.yml).
# FULLOCH_API_KEY=
# Optional image overrides for docker-compose.
# FULLOCH_IMAGE=ghcr.io/k0walski8/fulloch-hagp:latest
# FULLOCH_GPU_IMAGE=ghcr.io/k0walski8/fulloch-hagp:gpu-latest

# =============================================================================
# Web Search
# =============================================================================
# Optional override for SearXNG endpoint (also configurable in data/config.yml).
# SEARCH_SEARXNG_URL=http://192.168.50.153:30053
#
# Optional OpenRouter summarization for web searches.
# Enabled only when all values below are set.
# OPENROUTER_WEB_SEARCH_ENABLED=false
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# =============================================================================
# Spotify OAuth (Alternative to config.yml)
# =============================================================================
# If you prefer environment variables over config.yml for Spotify credentials:
# SPOTIPY_CLIENT_ID=your_spotify_client_id
# SPOTIPY_CLIENT_SECRET=your_spotify_client_secret
# SPOTIPY_REDIRECT_URI=http://localhost:8888/callback

# =============================================================================
# LG ThinQ (Alternative to config.yml)
# =============================================================================
# THINQ_ACCESS_TOKEN=your_thinq_access_token
# THINQ_CLIENT_ID=your_thinq_client_id

# =============================================================================
# Google Calendar (if using service account)
# =============================================================================
# GOOGLE_APPLICATION_CREDENTIALS=./data/service-account.json
